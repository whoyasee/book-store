spring:
  datasource:
    url: jdbc:tc:postgresql:9.6.8:///test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  flyway:
    enabled: false
